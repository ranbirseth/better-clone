/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['themes.pixelwars.org'], // Add any other domains you want to allow
    },
  };
  
  module.exports = nextConfig;

// next.config.js
module.exports = {
  images: {
    domains: ['media.better.com'],
  },
};
